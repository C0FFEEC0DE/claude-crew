{
  "id": "manager-debugger-tester-reviewer-zero-division",
  "suite": "full",
  "category": "bugfix",
  "related_agents": ["m", "dbg", "t", "cr"],
  "fixture": "python-math",
  "review_required": true,
  "docs_required": false,
  "verification_required": true,
  "prompt": "Use @m to coordinate. CRITICAL: You MUST launch @dbg, @t, and @cr as real subagent handoffs using the Agent tool — not just mentions in text. The benchmark validates actual SubagentStart hooks. After @m analyzes the task, launch @dbg for diagnosis, then @t for testing, then @cr for review. Fix divide() to raise ValueError on zero division. Add one test. Run pytest -q. Finish with review status.",
  "success_criteria": [
    "divide() raises ValueError on zero division",
    "One test added",
    "pytest -q passes",
    "Review status included"
  ],
  "must_not": [
    "Skip pytest -q",
    "Perform release or deploy work"
  ],
  "required_used_agents": ["m", "dbg", "t", "cr"]
}