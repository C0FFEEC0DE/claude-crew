{
  "id": "manager-bugbuster-tester-reviewer-zero-division",
  "suite": "full",
  "category": "bugfix",
  "related_agents": ["m", "bug", "t", "cr"],
  "fixture": "python-math",
  "review_required": true,
  "docs_required": false,
  "verification_required": true,
  "prompt": "Use @m to coordinate this bugfix. CRITICAL: You MUST launch @bug, @t, and @cr as real subagent handoffs using the Agent tool — not just mentions in text. The benchmark validates actual SubagentStart hooks. The first handoff must be to @m, then route in exact order: @bug for the fix, then directly to @t who MUST run pytest -q, then directly to @cr for review. Fix divide() to raise ValueError on zero division. Add one test. Finish with review status.",
  "success_criteria": [
    "divide() raises ValueError on zero division",
    "One test added",
    "pytest -q passes",
    "Manager routes directly from @bug to @t to @cr",
    "Review status included"
  ],
  "must_not": [
    "Skip pytest -q",
    "Perform release or deploy work"
  ],
  "required_used_agents": ["m", "bug", "t", "cr"]
}